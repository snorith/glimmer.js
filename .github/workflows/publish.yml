name: Publish Packages

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Release type'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    name: Bump Version & Publish
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          # Pin to 22.14.0 to avoid npm regression in 22.22.2 (MODULE_NOT_FOUND promise-retry)
          node-version: '22.14.0'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm for Trusted Publishing OIDC
        run: |
          npm install -g npm@latest
          npm --version

      - name: Install dependencies
        run: yarn install --frozen-lockfile --non-interactive

      - name: Build all packages
        run: yarn build

      - name: Bump version
        id: version
        run: |
          node scripts/bump-version.js ${{ inputs.release_type }}
          NEW_VERSION=$(node -p "require('./package.json').version")
          echo "version=$NEW_VERSION" >> "$GITHUB_OUTPUT"

      - name: Publish to npm
        run: node scripts/publish-packages.js --provenance

      - name: Commit and tag
        # Only tag after successful publish to avoid orphaned tags on failure
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Release v${{ steps.version.outputs.version }}"
          git tag "v${{ steps.version.outputs.version }}"
          git push
          git push --tags

